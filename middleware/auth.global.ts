export default defineNuxtRouteMiddleware((to, from) => {
  // console.log("TO:", to);
  // console.log("RROM:", from);
  // if (to.params.id === '1') {
  //   return abortNavigation()
  // }
  // if (to.path !== '/') {
  //   return navigateTo('/')
  // }
  // const isLogin = false;
  // if(isLogin){
  //   return navigateTo(to.fullPath)
  // }
  // return navigateTo('/login')
});
